/**
 * Root page server load.
 * Placeholder — data publik (activities, faqs, site_content) akan di-fetch
 * di sini saat halaman publik dikembangkan.
 */
export async function load() {
	return {};
}
